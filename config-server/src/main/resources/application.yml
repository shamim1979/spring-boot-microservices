spring:
  cloud:
    config:
      fail-fast: false
      server:
        native:
          search-locations:
            - classpath:/config
            - classpath:config/{application}
            - classpath:config/{profile}/{application}
  profiles:
    active: native
  security:
    user:
      name: user
      password: lims

server:
  port: 8888
  forward-headers-strategy: framework